pipeline {
    agent any

    stages {
        stage('ECHO') {
            steps {
                echo("This is the test from the Jenkinsfile after commiting the changes in the file")
            }

            
        }
    }
}
